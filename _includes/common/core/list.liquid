{%- include common/rest/site_pages.liquid -%}
{%- include common/rest/workdir.liquid workdir=page.dir -%}

{%- for item in workdir_files %}
- [{{ item.title | default: item.name }}]({{ item.url | relative_url }}) hi
{%- endfor -%}

{%- if include.all -%}
str
    {%- for workdir in workdir_dirs %}
	str4
	{% include common/core/_list.liquid %}
	end4
	{% endfor -%}
end
{%- endif -%}
